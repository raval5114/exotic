import 'package:exotic/controllers/auth/forgetPassword/sms_sending_controller.dart';
import 'package:flutter/material.dart';

class SmsSendingScreen extends StatelessWidget {
  const SmsSendingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: SmssendingscreenComponent());
  }
}
